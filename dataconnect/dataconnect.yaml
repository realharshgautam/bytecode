specVersion: "v1beta"
serviceId: "ByteCode"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "ByteCode-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
